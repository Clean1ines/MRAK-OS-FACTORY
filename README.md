
## 🛠️ Управление переменными окружения

Проект использует несколько файлов `.env`:

- `.env` — для локальной разработки (backend читает его через `python-dotenv`).
- `.env.test` — для запуска тестов (включая e2e).
- В production переменные задаются через окружение (например, в Docker/cloud).

**Важно:**  
- `DATABASE_URL` должна указывать на существующую БД.  
- `MASTER_KEY` используется для аутентификации, длина не менее 8 символов.  
- Для тестов используется БД на порту 5433 с пользователем `test` и паролем `test123` (поднимается через `docker-compose`).

Перед пушем выполняются проверки:
- Наличие обязательных переменных.
- Возможность подключения к БД.
- Прогон E2E-тестов.

Если тесты падают с ошибкой авторизации, проверьте, что в `.env.test` задан правильный `MASTER_KEY` и что он совпадает с используемым в тестах.
